

    var pierre = "pierre" ; 
    var feuille = "feuille" ; 
    var ciseaux = "ciseaux" ; 

    var tableau = [pierre , feuille , ciseaux]; 

    fonction comparer(choixjoueur)

    function comparaison(choix){ 
        var ordi = tableau[Math.floor(Math.random() * tableau.length)]
        if

}
    

